{
	"name": "@earendil-works/pi-decision-graph",
	"version": "0.84.1",
	"private": true,
	"description": "Temporal decision graph: captures the agent's decisions, why, and file changes into a project-local store.",
	"type": "module",
	"scripts": {
		"test": "vitest --run"
	},
	"dependencies": {
		"@earendil-works/pi-coding-agent": "^0.84.1"
	},
	"devDependencies": {
		"vitest": "4.1.9"
	},
	"engines": {
		"node": ">=22.19.0"
	}
}
